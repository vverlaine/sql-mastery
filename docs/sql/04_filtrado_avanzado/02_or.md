---
sidebar_position: 3
title: El Operador OR
---

# El Operador OR

## Cuando basta con que se cumpla una condición

`OR` amplía el resultado: un registro aparece si cumple **al menos una** de las condiciones. Es el operador de la unión.

```sql
SELECT id_venta, total, canal
FROM cbc_cas_dev.universidad.fact_ventas
WHERE canal = 'Online' OR canal = 'Directo';
```

Esta consulta devuelve todas las ventas online y todas las directas. No importa cuál de las dos condiciones se cumpla, si alguna lo hace, el registro aparece.

---

## AND vs OR: la distinción crítica

Este es uno de los errores más comunes en SQL, y en análisis en general:

- `AND` **reduce** el resultado: el registro debe cumplir todo.
- `OR` **amplía** el resultado: el registro debe cumplir algo.

Pregunta siempre: *¿necesito que se cumplan todas las condiciones o al menos una?* La respuesta determina el operador.

---

## 🎯 Tarea

Recupera el `id_venta` de las ventas que pertenecen al canal `'Online'` o `'Directo'`.

> ⚠️ Recuerda: `'Online'` y `'Directo'` deben escribirse exactamente así, con mayúsculas correctas.

<details>
<summary>Ver solución</summary>

```sql
SELECT id_venta
FROM cbc_cas_dev.universidad.fact_ventas
WHERE canal = 'Online' OR canal = 'Directo';
```

</details>

---

## Desafío: Productos específicos

Recupera el `id_venta` de las ventas del producto `'SKU-0501'` o `'SKU-0503'`.

<details>
<summary>Ver solución</summary>

```sql
SELECT id_venta
FROM cbc_cas_dev.universidad.fact_ventas
WHERE id_producto = 'SKU-0501' OR id_producto = 'SKU-0503';
```

</details>

---

*Universidad Nexus — Curso de SQL*
